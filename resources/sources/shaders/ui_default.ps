#include "ui_default.h"

#include "samplers.h"

Texture2D t_texture : register( t0 );

float4 main( in vertex_output input ) : SV_Target0
{
	return input.color * t_texture.Sample( s_ui_sampler, input.tex_coords );
}